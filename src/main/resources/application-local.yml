spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zemyanadlan?createDatabaseIfNotExist=true
    username: zemya
    password: zemya123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration/mysql

  security:
    user:
      name: admin
      password: "1234"

logging:
  level:
    org:
      springframework:
        web:
          servlet:
            DispatcherServlet: DEBUG
          filter:
            CommonsRequestLoggingFilter: DEBUG
      hibernate:
        SQL: DEBUG
        orm:
          jdbc:
            bind: TRACE