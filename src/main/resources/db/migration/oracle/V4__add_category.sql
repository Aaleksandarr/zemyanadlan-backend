CREATE TABLE categories (
    id NUMBER(19) GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(100) NOT NULL,
    slug VARCHAR2(100) NOT NULL,
    category_scope VARCHAR2(50) NOT NULL
    );